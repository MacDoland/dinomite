const characters = {
    REX: 0,
    HUMPHRY: 1
}

const characterNames = {}

characterNames[characters.REX] = 'rex';
characterNames[characters.HUMPHRY] = 'humphry';

export { characters, characterNames };